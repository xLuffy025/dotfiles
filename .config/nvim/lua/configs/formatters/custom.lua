-- Configuración personalizada de formatters
-- Añade aquí tus formatters personalizados

return {
  formatters = {
    -- Ejemplo: añadir un formatter personalizado
    -- my_custom_formatter = {
    --   command = "my-formatter",
    --   args = { "--option", "$FILENAME" },
    -- },
  },

  formatters_by_ft = {
    -- Ejemplo: añadir formatter para un tipo de archivo específico
    -- python = { "black" },
  },
}
